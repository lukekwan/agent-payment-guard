# Agent Payment Control

CONTRACT_STATUS=PROPOSED_CANDIDATE

Verified x402 payment-intent evaluation. Preserve lowercase allow/review/deny and signer directive values.

## Boundary

This first-release package documents only the allowlisted endpoints. It does not deploy code, change runtime/OpenAPI, issue credentials, promise a sandbox, enable Test it, or remap decision vocabularies.
