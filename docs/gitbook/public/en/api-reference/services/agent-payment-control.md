# Agent Payment Control

Evaluate a payment intent and preserve the current service vocabulary: `allow`, `review`, or `deny`.

## First-release endpoint

`GET /v1/x402/agent/payment-risk-gateway` — verified current x402 wrapper. The repository contains a price conflict that must be resolved before publication; no OAuth/API-key scope is claimed.
