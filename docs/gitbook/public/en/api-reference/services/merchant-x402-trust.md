# Merchant & x402 Trust

Assess a merchant wallet and inspect an x402 endpoint before an agent purchases.

## First-release endpoints

- `GET /v1/x402/base/merchant-trust` — verified x402 wrapper; catalog price 0.03 USDC.
- `GET /v1/x402/web/endpoint-preflight` — verified x402 wrapper; catalog price 0.005 USDC.
