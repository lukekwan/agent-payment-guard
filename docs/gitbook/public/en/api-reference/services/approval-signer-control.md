# Approval & Signer Control

Produce an explicit signer directive from a bound decision and action. This service never signs, returns key material, or mints approval.

## First-release endpoint

`POST /v1/approval-signer/evaluate` — proposed candidate, not deployed. Authentication, scope, billing, and price are TBD.
