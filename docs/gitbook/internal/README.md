# Internal documentation boundary

This directory is intentionally outside the public GitBook root. Operational runbooks, internal approval identifiers, unpublished rollout details, and internal API specifications belong here and must never be uploaded by the public sync tool.

The public sync allowlist is limited to `docs/gitbook/public/` and `docs/openapi/signgate-public-v0.1.openapi.json`.
