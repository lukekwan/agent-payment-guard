[project]
name = "signgate-agent-buyer-policy-kit"
version = "0.1.0"
description = "Agentic commerce policy evaluator for mandate, merchant, buyer-role, and signer-directive preflight."
requires-python = ">=3.10"
license = { text = "UNLICENSED" }
authors = [{ name = "SignGate" }]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
